services:
  spot-dl:
    build: .
    image: spot-dl:local
    ports:
      - "5555:5555"
    environment:
      SPOTDL_CONFIG_ROOT: /config
      SPOTDL_MUSIC_DIR: /music
    volumes:
      - ./spotdl-config:/config
      - ./spotdl-music:/music
    restart: unless-stopped
